name: cosmotheka
channels:
  - conda-forge
dependencies:
  - python>=3.10
  # Development
  - wheel
  - pytest
  - pytest-cov
  - coveralls
  - flake8
  # User
  - cython
  - numpy<2.4  # TODO: Update to 2.4 in a future PR, when CCL supports it.
  - scipy
  - scikit-learn
  - flake8
  - astropy
  - healpy
  - pandas
  - namaster>=3.0
  - pyccl>=3.2.0
  - camb
  - sacc
  - fitsio
  - healpy
  - pixell
  # - pip
  # - pip:
